# Docker ignore file for Expeditious Reader
# Excludes files that don't need to be in the Docker build context

# Documentation
*.md
!docker/README.md
CHANGELOG.md
LICENSE

# Git
.git/
.gitignore
.gitattributes

# IDE
.vscode/
.idea/
*.iml

# Build artifacts (we'll copy them explicitly when needed)
build/
releases/
.dart_tool/
.flutter-plugins-dependencies

# Tests
test/
coverage/

# Scripts (not needed in container)
*.sh
*.ps1
!docker/*.sh

# Platform-specific (not needed for web)
android/
ios/
linux/
macos/
windows/

# Temporary files
*.log
*.tmp
*.temp
*.bak
*.backup
*~
.DS_Store

# Docker files (avoid recursion)
Dockerfile*
docker-compose*.yml
.dockerignore
docker-build.sh

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml

# Environment files
.env*
!.env.example
